<Page x:Class="final_pr15.Pages.ManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:final_pr15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ManagerPage"
      Background="{StaticResource BackgroundBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление категориями/брендами/тегами" Foreground="{StaticResource PrimaryBrush}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="White" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Добавить" Click="AddButton_Click" Background="{StaticResource SecondaryBrush}" Foreground="White" Width="100" Height="35" Margin="5"/>
                <Button Content="Изменить" Click="EditButton_Click" Background="{StaticResource SecondaryBrush}" Foreground="White" Width="100" Height="35" Margin="5" IsEnabled="False" x:Name="EditButton"/>
                <Button Content="Удалить" Click="DeleteButton_Click" Background="{StaticResource SecondaryBrush}" Foreground="White" Width="100" Height="35" Margin="5" IsEnabled="False" x:Name="DeleteButton"/>
                <Button Content="Назад" Click="Back_Click" Width="100" Height="35" Foreground="White" Background="{StaticResource PrimaryBrush}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <Border x:Name="AddPanel" Grid.Row="2" Background="White" Padding="10" 
                Margin="0,0,0,10" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewNameBox" Grid.Column="0" />
                <Button Grid.Column="1" Content="Сохранить" Click="SaveAdd_Click" Background="{StaticResource SecondaryBrush}" Foreground="White" Width="80" Margin="5,0,0,0"/>
                <Button Grid.Column="2" Content="Отмена" Click="CancelAdd_Click" Width="80" Margin="5,0,0,0" Background="{StaticResource PrimaryBrush}" Foreground="White"/>
            </Grid>
        </Border>

        <Border x:Name="EditPanel" Grid.Row="3" Background="White" Padding="10" 
                Margin="0,0,0,10" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="EditNameBox" Grid.Column="0" />
                <Button Grid.Column="1" Content="Сохранить" Click="SaveEdit_Click" Background="{StaticResource SecondaryBrush}" Foreground="White" Width="80" Margin="5,0,0,0"/>
                <Button Grid.Column="2" Content="Отмена" Click="CancelEdit_Click" Width="80" Margin="5,0,0,0" Background="{StaticResource PrimaryBrush}" Foreground="White"/>
            </Grid>
        </Border>

        <ListView x:Name="ItemsList" Grid.Row="4" 
                  SelectionChanged="ItemsList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Название" Width="350" DisplayMemberBinding="{Binding Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
